<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".PostingsTestActivity">

    <Button
        android:id="@+id/btnallpost"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="All Post"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btnvote"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="vote"
        app:layout_constraintEnd_toStartOf="@+id/btnallpost"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/btntips"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="tips"
        app:layout_constraintEnd_toStartOf="@+id/btnvote"
        app:layout_constraintTop_toTopOf="parent" />

    <ListView
        android:id="@+id/lvpostlist"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@+id/btntips" />

</androidx.constraintlayout.widget.ConstraintLayout>